<Window x:Class="RadioMonitor.FingerprintManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fingerprint DB Viewer" Height="640" Width="960"
        WindowStartupLocation="CenterOwner">
  <DockPanel>
    <ToolBar DockPanel.Dock="Top">
      <TextBlock Text="DB:" VerticalAlignment="Center" Margin="0,0,6,0"/>
      <TextBox x:Name="DbPathBox" Width="520" IsReadOnly="True" />
      <Separator/>
      <TextBlock Text="Table:" VerticalAlignment="Center" Margin="10,0,4,0"/>
      <ComboBox x:Name="TablesCombo" Width="220" SelectionChanged="TablesCombo_SelectionChanged"/>
      <TextBlock Text="Rows:" VerticalAlignment="Center" Margin="10,0,4,0"/>
      <TextBox x:Name="LimitBox" Width="70" Text="1000"/>
      <Button Content="Refresh" Margin="8,0,0,0" Click="Refresh_Click"/>
      <Separator/>
      <TextBlock Text="Filter (contains):" VerticalAlignment="Center" Margin="8,0,4,0"/>
      <TextBox x:Name="FilterBox" Width="220" TextChanged="FilterBox_TextChanged" />
    </ToolBar>

    <Grid Margin="6">
      <DataGrid x:Name="Grid"
                AutoGenerateColumns="True"
                CanUserAddRows="False"
                IsReadOnly="True"
                HeadersVisibility="Column"
                EnableRowVirtualization="True"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.ScrollUnit="Pixel"
                Margin="0,0,0,28"/>
      <StatusBar DockPanel.Dock="Bottom" VerticalAlignment="Bottom">
        <StatusBarItem>
          <TextBlock x:Name="StatusText" Text="Ready"/>
        </StatusBarItem>
      </StatusBar>
    </Grid>
  </DockPanel>
</Window>
